import { Building2 } from 'lucide-react'
import type { PageMeta } from '@/components/common/site-scoped-route'

/** Identité de la page Localisations — source unique. */
export const PAGE_META: PageMeta = {
  titre: 'Localisations',
  description: 'Bâtiments, niveaux et locaux du site.',
  hint: 'Choisis un site pour voir ses localisations.',
  icone: Building2,
}
